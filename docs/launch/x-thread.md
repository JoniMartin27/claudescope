# X / Twitter thread

Post a few days after HN/Reddit to ride momentum (don't repost — reframe). One killer screenshot per tweet. Always include `npx claudescope-cli` + the repo link in tweet 1 and the last tweet.

**1/ (hook + asset: search-with-highlighting GIF)**
```
ccusage tells you how much you spent on Claude Code.
I wanted to *search what I actually did* — so I built ClaudeScope.

Full-text search across every prompt, reply & tool call you've ever run,
then click to read the session. `npx claudescope-cli`. 0 deps, 100% local, MIT. 🧵
```

**2/ (asset: the dashboard / token-mix donut)**
```
It also shows the usage you can't otherwise see: tokens, an API-equivalent cost
estimate, and what caching saved you. For me: «97%» of tokens were cache reads —
that's «~$14k» the cache saved at list rates. (Estimate, not a bill — I'm on Max.)
```

**3/ (asset: the heatmap)**
```
A weekday × hour heatmap of when you actually code with Claude.
Mine peaks «Tuesday 6pm». It'll also tell you which project eats your time and
where Claude went the wrong way (sessions you stopped mid-reply).
```

**4/ (asset: the Wrapped card)**
```
And a "Wrapped" card — your stats as a shareable image, rendered 100% locally
and anonymized (no project names, no prompts). Flex your stats without uploading
a single byte. The one-click privacy version of the year-in-review.
```

**5/ (asset: source bar showing Codex/Copilot)**
```
Bonus: if you also use Codex / Cursor / Aider / Gemini / Copilot CLI, it picks
up their logs too — search across all your agents in one place.

Zero install: `npx claudescope-cli`
Repo (MIT): github.com/JoniMartin27/claudescope
```

> Variant hook for a quote-tweet of any "my Claude bill is insane" post:
> "you can actually see where it all goes (and what caching saves you) with `npx claudescope-cli` — 100% local, nothing uploaded."
