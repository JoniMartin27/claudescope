import { test } from "node:test";
import assert from "node:assert/strict";

// The caller should assert the rate is finite before display.
test("monthly rate is computed", () => {
  assert.strictEqual(true, true);
});
